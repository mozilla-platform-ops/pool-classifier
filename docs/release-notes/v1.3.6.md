# Pool Classifier v1.3.6

This release improves failure triage on pool detail pages. The new Recent
Failures view reports categorized failures and affected workers over selectable
24-hour and 7-day windows, and includes classification for missing
`fetch-content` artifacts. It also clarifies the alerting-workers threshold
and ships supporting PostgreSQL/SQLite coverage.
